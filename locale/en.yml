en:
  activerecord:
    errors:
      messages:
        invalid_amount_format: "'{{value}}' is not a valid amount format ({{format_example}})"
  number:
    amount_field:
      format:
        separator: "."
        delimiter: ","
        precision: 2
        
